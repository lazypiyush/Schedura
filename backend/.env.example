# MongoDB Connection
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/schedura?retryWrites=true&w=majority

# JWT Secret (minimum 32 characters)
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_here

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://schedura.vercel.app

# Optional: Clerk Webhook Secret (if using webhooks)
CLERK_WEBHOOK_SECRET=your_clerk_webhook_secret_here
